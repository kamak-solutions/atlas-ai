services:
  ai-model:
    image: python:3.10-slim
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - PYTHONPATH=.
    command: sh -c "pip install --user pytest && ~/.local/bin/pytest tests/"